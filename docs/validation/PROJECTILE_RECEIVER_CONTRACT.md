# Projectile Receiver Contract Validation

Validation-only marker for the live fix already committed directly to `main`.

The target revision routes the shared projectile scene through `GenericProjectileSafe`, normalizes void/scalar receiver returns into a result dictionary, and adds a focused regression that requires a Blast Barrel's `Nil` return to be classified as handled while detonation, HitStop release, and frame advancement continue.
