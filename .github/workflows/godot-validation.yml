name: Godot validation

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: godot-validation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Focused aerial reactions validation
    runs-on: ubuntu-latest
    timeout-minutes: 12

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Godot 4.6
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.6.0
          use-dotnet: false

      - name: Import project headlessly
        run: godot --headless --path . --editor --quit

      - name: Run airborne hit reaction smoke test
        run: godot --headless --path . scenes/tests/hit_reaction_smoke_test.tscn

      - name: Boot combo arena headlessly
        run: godot --headless --path . scenes/levels/prototypes/prototype_weapon_combat_arena_v1.tscn --quit-after 5
