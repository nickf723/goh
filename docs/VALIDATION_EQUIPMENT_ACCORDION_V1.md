# Equipment Accordion v1 Validation Mirror

This branch exists only to run pull-request Godot validation against the equipment accordion code already committed directly to `main`.

It should be closed without merging after validation. The feature code is not duplicated here.
