# Refined Magic and Projectile Validation Mirror

This branch exists only to run pull-request validation against code already committed directly to `main`.

It validates the 2×4 spell grid, separate 3×1 augmentation strip, cursor/D-pad ownership handoff, and center-screen projectile convergence.

Close without merging after validation.
