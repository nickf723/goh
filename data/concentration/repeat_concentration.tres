[gd_resource type="Resource" script_class="ConcentrationEffectDefinition" format=3]

[ext_resource type="Script" path="res://scripts/concentration/concentration_effect_definition.gd" id="1_concentration"]

[resource]
script = ExtResource("1_concentration")
effect_id = "repeat_concentration"
display_name = "Repeat"
description = "Reserve 20 percent of Grace's mana capacity to sustain a time-lagged echo. The echo reenacts Grace's movement, attacks, and supported spell timelines one second later."
mana_reservation_fraction = 0.20
free_elements = Array[String]([])
discounted_elements = {}
effect_tags = Array[String](["concentration", "time", "repeat", "echo", "ally", "delayed_copy", "stackable_concentration"])
