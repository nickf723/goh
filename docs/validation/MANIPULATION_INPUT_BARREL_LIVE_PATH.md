Validation-only marker for the exact production repair on main.

The focused regression now sends real controller events through the input system and fires an actual charged GenericProjectile into a Blast Barrel, covering default-action suppression, HitStop release, safe base-manager actors, and deferred chains.
