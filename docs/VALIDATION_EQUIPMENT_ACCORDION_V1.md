# Equipment Accordion v2 Validation Mirror

This branch exists only to run pull-request Godot validation against the geometry-navigation, persistent Grace layout, four stat quadrants, and right-stick menu cursor already committed directly to `main`.

It should be closed without merging after validation. The feature code is not duplicated here.
