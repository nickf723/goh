# Mob Engine Foundation v1 evaluator validation

Validation-only marker for the exact corrected evaluator revision already committed directly to `main`.

This branch must be closed without merging after CI completes.
