name: Godot validation

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: godot-validation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Focused wilds stale-reference validation
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install Godot 4.6
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.6.0
          use-dotnet: false

      - name: Import project headlessly
        run: godot --headless --path . --editor --quit

      - name: Run wilds stale-reference smoke test
        run: godot --headless --path . scenes/tests/wilds_expedition_smoke_test.tscn

      - name: Boot wilds expedition headlessly
        run: godot --headless --path . scenes/levels/prototypes/prototype_wilds_expedition_v1.tscn --quit-after 6

      - name: Boot combo arena regression
        run: godot --headless --path . scenes/levels/prototypes/prototype_weapon_combat_arena_v1.tscn --quit-after 5
